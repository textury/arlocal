import { exec } from 'child_process';
import { port } from '../test-setup';

describe('CHUNK', () => {
  it('', async () => {
    // await exec(`yarn arkb deploy ./__tests__/data/images/ --gateway http://localhost:${port} --debug`);
  });
});
